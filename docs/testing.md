# Testing

The repository relies on the Amen testing framework to validate the functional behavior of API extraction utilities. The test suite exercises various logical paths and edge cases against a localized fixture API description to ensure correct output extraction and transformation.

To execute the test suite, run the following command:

```bash
npx genie test
```
